local neogen = require("neogen")

neogen.setup({
	--if you want to disable Neogen
	enabled = true,
	-- (default: true) automatic jump (with insert mode) on inserted
	-- annotation
	input_after_comment = true,
})
